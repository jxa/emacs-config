(add-to-list 'load-path (extern-path "worklog"))
(require 'worklog)
;; (setq worklog-automatic-login t)
;; (add-hook 'emacs-startup-hook
;;           (function (lambda () (worklog-add-entry-login))))
;; (add-hook 'kill-emacs-hook
;;           (function (lambda () 
;;                       (worklog-add-entry-logout)
;;                       (worklog-finish))))

